Use case: infographic-diagram
Asset type: TaskBeacon task flow diagram
Primary request: Create a clean, publication-ready task flow diagram as a timeline collection for the behavioral task described below.

Task: Loss Aversion / Framing Task
Construct: loss aversion / framing effects / risky choice
Rows/conditions:
- Gain frame: sure keep amount vs risky keep-all/keep-none option.
- Loss frame: sure loss amount vs risky full-loss/no-loss option.
- Mixed frame: sure amount vs mixed gain-loss lottery.

Timeline phases:
- Gain frame: Fixation (400-700 ms; no response; +) -> Decision (4000 ms; press F=safe / J=gamble; sure keep vs keep-all/keep-none gamble) -> Feedback (700 ms; no response; chosen option / timeout) -> ITI (400-800 ms; no response; +)
- Loss frame: Fixation (400-700 ms; no response; +) -> Decision (4000 ms; press F=safe / J=gamble; sure loss vs full-loss/no-loss gamble) -> Feedback (700 ms; no response; chosen option / timeout) -> ITI (400-800 ms; no response; +)
- Mixed frame: Fixation (400-700 ms; no response; +) -> Decision (4000 ms; press F=safe / J=gamble; sure amount vs mixed gain-loss lottery) -> Feedback (700 ms; no response; chosen option / timeout) -> ITI (400-800 ms; no response; +)

Visual requirements:
- White background, landscape orientation, crisp dark text, restrained condition accent colors.
- One horizontal row per frame condition.
- Each row contains 4 participant-screen snapshots connected by a subtle arrow.
- Each screen snapshot shows participant-visible screen content only.
- Use gray participant-screen boxes, thin black arrows, consistent row spacing, and subtle row separators.
- Place timing labels under each screen in compact text.
- Place frame labels at the left of each row.
- Use short labels only; avoid paragraphs inside the image.
- Make all text legible at normal document preview size.
- Leave a clean blank header band across the top 18-20% of the image.

Accuracy constraints:
- Do not invent phases, stimuli, condition names, keys, rewards, lottery resolution, or timings.
- Do not add people, lab equipment, decorative scenes, logos, or unrelated icons.
- Do not draw the task title, construct subtitle, any logo, watermark, brand mark, or `TaskBeacon` text inside the generated image.
- Draw only the timeline content below the blank header band.
- Preserve these exact terms where used: Gain frame, Loss frame, Mixed frame, F=safe, J=gamble, 400-700 ms, 4000 ms, 700 ms, 400-800 ms.

Style:
TaskBeacon scientific infographic style: clean vector-like raster image, organized spacing, gray screen boxes, restrained color accents, and a blank header-safe area.
